import 'package:flutter/material.dart';

/// Mobile layout wrapper for SettingsPage — full width
class SettingsPageMobile extends StatelessWidget {
  final Widget child;

  const SettingsPageMobile({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
