import 'package:flutter/material.dart';

/// Mobile layout wrapper for ReportsPage — full width
class ReportsPageMobile extends StatelessWidget {
  final Widget child;

  const ReportsPageMobile({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
